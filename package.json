{
  "name": "garageos-web",
  "version": "1.0.0",
  "private": true,
  "description": "Поддержка GarageOS — статичный сайт. Источник истины — content/*.md, index.html генерируется build.js.",
  "scripts": {
    "build": "node build.js"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^15.0.0"
  }
}
